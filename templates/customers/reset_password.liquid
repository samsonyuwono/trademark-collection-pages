{%- assign force_secondary_background = true -%}

<div class="page__content-wrapper">
  <div class="container">
    <div class="page__header">
      <h1 class="page__title h1">{{ 'customer.reset_password.title' | t }}</h1>
    </div>

    <div class="box box--mini">
      <div class="box__content box__content--large-padding box__content--centered">
        {%- form 'reset_customer_password', class: 'form form--shrinked' -%}
          <p class="form__instructions">{{ 'customer.reset_password.instructions' | t }}</p>

          {%- if form.errors -%}
            <div class="alert alert--error">
              <span class="alert__title">{{ 'general.forms.errors' | t }}</span>
              {%- include 'form-errors' -%}
            </div>
          {%- endif -%}

          <div class="form__control">
            <input type="password" class="form__input" name="customer[password]" required="required" placeholder="{{ 'customer.reset_password.password' | t }}">
          </div>

          <div class="form__control">
            <input type="password" class="form__input" name="customer[password_confirmation]" required="required" placeholder="{{ 'customer.reset_password.password_confirmation' | t }}">
          </div>

          <div class="box__button-wrapper button-wrapper">
            <button type="submit" class="button button--enforce-min-width button--primary">{{ 'customer.reset_password.submit' | t }}</button>
          </div>
        {%- endform -%}
      </div>
    </div>
  </div>
</div>